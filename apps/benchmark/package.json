{
  "name": "@shadejs/benchmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench --run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sarthakdev143/core": "workspace:*",
    "@sarthakdev143/runtime": "workspace:*"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "vitest": "^2.1.8"
  }
}
